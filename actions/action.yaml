name: First composite action

runs:
  using: composite
  defaults:
  run:
    shell: bash

  steps:
    - name: ls 1
      run: |
        ls -l

    - name: Make dir
      run: |
        mkdir test_dir_composite
        echo "secret=something" > test.env
        cat test.env
      
    - name: ls 2
      run: | 
        ls -l